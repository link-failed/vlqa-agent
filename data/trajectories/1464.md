Thoughts:
I should firstly load the fees data. I notice manual.md mentions that when a field is an empty list or a null value, it means it is applicable for all cases. So I should considering null values and empty lists for all relevant fields, especially for fee matching. So I need to filter the entries based on the criteria: account_type should contain 'R' or be None/empty, and aci should contain 'B' or be None/empty. For entries that meet these criteria, I will extract their 'ID' values, format them as a comma-separated list, and return the result. If no IDs match the criteria, I will return 'Not Applicable'.

Code:
```py
import json

# Load the fees data
with open('./data/context/fees.json', 'r') as file:
    fees_data = json.load(file)

# Define the filtering criteria
account_type = 'R'
aci = 'B'

fee_ids = []

for entry in fees_data:
    # Check if the entry has the required fields
    if 'account_type' in entry and 'aci' in entry and 'ID' in entry:
        # Check if account_type contains 'R' or is None/empty, and aci contains 'B' or is None/empty
        if ((entry['account_type'] is None or entry['account_type'] == [] or 'R' in entry['account_type']) and \
            (entry['aci'] is None or entry['aci'] == [] or 'B' in entry['aci'])):
            fee_ids.append(str(entry['ID']))

# Format the answer
if fee_ids:
    answer = ', '.join(fee_ids)
else:
    answer = 'Not Applicable'

final_answer(answer)

```
<end_code>
